/**
 * Configuration exports
 * 
 * This file serves as the main entry point for all configuration
 * modules in the application.
 */

export { featureFlags, isFeatureEnabled, getAllFeatureFlags } from './feature-flags';
export type { FeatureFlags } from './feature-flags';
